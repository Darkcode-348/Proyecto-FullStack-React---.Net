export const UrlApi = {
  Vite_Api_url: import.meta.env.VITE_Api_url, // Asegúrate de que la variable sea escrita en mayúsculas correctamente
};
export const CORS = { headers: { 'Access-Control-Allow-Origin': '*' } }; // cors
